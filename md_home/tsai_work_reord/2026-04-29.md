# 工作日誌 — 2026-04-29

**專案**：GLŌW 輔大美妝交流平台  
**負責人**：alfie + 協作  
**修改檔案**：`backend/routes/questions.js`、`backend/routes/notifications.js`、`backend/index.js`、`frontend/src/QA.js`、`frontend/src/Navbar.js`、`frontend/src/components/SkinQuiz.js`、`frontend/src/App.js`

---

## 工作摘要

### 1. 問答回覆通知系統

**後端（`routes/notifications.js`）**：
- `GET /api/notifications/unread` — 查詢用戶未讀通知數量
  - 參數：`user_id`、`since`（上次查看時間）
  - 邏輯：有人回覆我的問題時觸發通知

**前端（`Navbar.js`）**：
- 問答導覽連結右上角顯示未讀紅色 badge
- 每 10 秒自動輪詢一次（`setInterval`）
- 進入問答頁後更新 `lastSeenQA` 至 `localStorage`，badge 自動清除

---

### 2. QA 頁面側欄更新

- 右側 Sidebar 新增三層回答說明（AI 分析 / 社群回覆 / 專業版主）的視覺區塊
- 熱門標籤、相關問題快速入口補完

---

### 3. 膚質測驗元件升級（`SkinQuiz.js`）

- 拆為獨立 `components/SkinQuiz.js`，不再內嵌於 Register 頁
- 新增客製化下拉選單元件 `components/Select.js`，風格對齊全站設計系統
- 測驗題目 UI 改善：選項 chip 可複選，已選項高亮顯示

---

### 4. 後端 API 分頁（`questions.js`、`products API`）

- 問題列表 API 新增 `page` / `limit` 分頁參數
- 產品列表 API 同步加入分頁支援
- 回傳格式新增 `total` 總筆數，方便前端計算頁數

---

### 5. 前端效能優化（`App.js`）

- 路由層級改用 `React.lazy` + `Suspense` 懶加載
- 非首屏頁面（Community、QA、Admin 等）改為按需載入
- 減少初始 bundle size，加快首頁載入速度

---

### 6. 部署準備

- `frontend/vercel.json` 補充 `outputDirectory: "build"`
- `.gitignore` 確認 `node_modules/` 與 `backend/.env` 均已排除
- 初始化根目錄 `package.json`（為後續 monorepo 管理準備）

---

*GLŌW Dev Log · alfie*
