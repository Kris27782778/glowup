# 工作日誌 — 2026-05-01

**專案**：GLŌW 輔大美妝交流平台  
**負責人**：alfie  
**修改檔案**：`backend/routes/admin.js`、`backend/routes/auth.js`、`frontend/src/Admin.js`、`frontend/src/Register.js`、`frontend/src/Dashboard.js`、`frontend/src/ProductDB.js`、`frontend/src/Navbar.js`、`frontend/src/Hero.js`、`frontend/src/Community.js`、`frontend/src/QA.js`、`frontend/src/Login.js`、`frontend/src/index.js`、`frontend/src/responsive.css`（新增）、`frontend/vercel.json`、`backend/nixpacks.toml`（新增）、`package.json`

---

## 工作摘要

### 1. 管理後台強化（`Admin.js`、`routes/admin.js`）

- 新增封鎖 / 解鎖用戶功能（`PATCH /api/admin/users/:id/ban`）
- 管理介面顏色與樣式全面更新，區分危險操作（紅色）與一般操作
- 用戶搜尋改為支援「姓名」欄位查詢

---

### 2. 用戶姓名欄位（`auth.js`、`Register.js`、`Admin.js`）

- 資料庫 `users` 表新增 `name` 欄位
- 註冊 Step 1 加入「姓名」輸入框
- 管理後台用戶列表顯示姓名，並支援以姓名搜尋

---

### 3. Dashboard 數據視覺化優化（`Dashboard.js`）

**HBar（水平長條圖）**：
- 每個長條右端新增對應數值顯示
- 底部新增所有欄位的「總計」加總列

**WeeklyBar（每週長條圖）**：
- 同步新增總計顯示
- 改善 Y 軸刻度計算，數字不再重疊

---

### 4. 響應式設計（RWD）— 全站手機 / 平板支援

**新增 `responsive.css`**，以 CSS media query 覆蓋各頁面 inline 樣式。

| 斷點 | 對應裝置 |
|------|---------|
| `≤ 1024px` | 平板 |
| `≤ 768px` | 手機 |

**各頁面改動**：

| 頁面 | 手機版調整 |
|------|-----------|
| Navbar | 導覽列隱藏 → 漢堡選單 ☰，點擊展開全頁選單（含發文、登出） |
| 首頁（未登入）| Hero 雙欄 → 單欄堆疊；字體 64px→40px；功能卡 3 欄→1 欄 |
| 首頁（已登入）| main + sidebar 橫排→直排 |
| 社群討論 | Hero 縮排；搜尋+發文換行；sidebar 移至下方 |
| 問答 QA | sidebar 移至下方 |
| 登入 / 註冊 | 左側品牌裝飾隱藏，表單全寬顯示 |

---

### 5. 產品資料庫與註冊頁優化

- `ProductDB.js`：移除多餘 `useEffect` 重複呼叫，改善搜尋時機邏輯
- `Register.js`：清理不必要的狀態管理，簡化 OTP 驗證流程

---

### 6. 部署衝突修復（最優解）

偵測並修復 4 個部署衝突：

| # | 問題 | 修復方式 |
|---|------|---------|
| 1 | `frontend/vercel.json` 缺少 SPA rewrites → 重整頁面 404 | 合併 `public/vercel.json` 的 rewrites 規則 |
| 2 | 根目錄無 `start` script → Railway 找不到啟動指令 | 新增 `backend/nixpacks.toml` 明確指定 `node index.js` |
| 3 | `react-router-dom` v7（root）vs v6（frontend）版本衝突 | 移除根目錄 `package.json` 的 react-router-dom |
| 4 | `PORT` 不能加入 Railway Variables | 手動確認並移除 |

---

## 目前部署狀態

- **GitHub**：`main` 分支最新，所有修復已推上
- **Railway（後端）**：Root Directory 設為 `backend`，nixpacks.toml 驅動啟動
- **Vercel（前端）**：Root Directory 設為 `frontend`，`REACT_APP_API_URL` 填入 Railway URL
- **Supabase**：雲端 PostgreSQL，無需額外部署

## 待辦 / 下一步

- [ ] 完成 Railway 後端部署，取得公開 URL
- [ ] Vercel 設定 `REACT_APP_API_URL` 完成前端部署
- [ ] 驗證部署後各功能正常（登入、社群、問答、收藏）
- [ ] CORS 限制改為只允許 Vercel domain（目前全開）

---

*GLŌW Dev Log · alfie*
