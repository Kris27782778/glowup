# 工作日誌 2026-04-09

**專案：** GLŌW 輔大美妝交流平台  
**負責人：** Tsai  
**日期：** 2026 年 4 月 9 日（四）

---

## 今日完成項目

### 1. 登入 / 註冊頁面保留 Navbar & Footer

- 移除 `STANDALONE_ROUTES` 機制，所有頁面統一渲染 Navbar + Footer
- 登入與註冊頁面調整上方偏移：`marginTop: '64px'`、`minHeight: 'calc(100vh - 64px)'`

---

### 2. 登入 / 註冊頁面左右欄平衡

- 以登入頁為設計基準，左側 50% 深色品牌視覺區、右側 50% 表單區
- 左側統一結構：
  - GLŌW 大字（Cormorant 96px）
  - 玫瑰陶土分隔線（48px）
  - 斜體副標「了解你擦在臉上的一切」
  - 三大特色條列（小圓點 accent 色 + 文字，無 emoji）
  - 底部標語 CLARITY IS BEAUTY
  - 三顆裝飾圓圈（右上、左下、右下）

---

### 3. 登入頁 GLŌW 全頁 Splash 動畫（`Login.js`）

- 登入頁進入時顯示全螢幕 Splash 蓋板（`position: fixed; inset: 0; zIndex: 999`）
- 動畫序列：
  1. GLŌW 字樣淡入（`glow-splash-text`，800ms）
  2. 分隔線從 0 展開至 48px（`glow-splash-line`，500ms）
  3. 副標淡入（`glow-splash-sub`，600ms）
  4. 2000ms 後觸發退場：蓋板整體向上滑出（`glow-splash-rise`，800ms）
  5. 2800ms 後 `splash` state 改為 `'done'`，蓋板從 DOM 移除
- 登入成功後跳轉至 `/`（改自原 `/dashboard`）

---

### 4. 電子郵件驗證步驟（`Register.js`）

- 新增第 3 步驟「電子郵件驗證」，共 4 步驟：
  `基本資料 → 帳號設定 → 電子郵件驗證 → 膚質設定`
- OTP 輸入 UI：
  - 6 格獨立 `<input>`，自動前進焦點、退格回退、支援貼上
  - `useRef` Array 管理各格 ref
- 60 秒重發倒數計時（`setInterval` + `useRef` 清除）
- 介面說明：寄送至 `{學號}@cloud.fju.edu.tw`，TODO 預留後端 API 接入點
- 膚質測驗移至第 4 步驟（原第 3 步）
- 註冊成功頁按鈕「進入 GLŌW」導向 `/`

---

### 5. 全站動畫系統（`animations.css` + `useReveal.js`）

#### `animations.css`（新建）

| 分類 | 內容 |
|---|---|
| Keyframes | `glow-page-in`、`glow-fade-up`、`glow-fade-in`、`glow-scale-in`、`glow-slide-left/right`、`glow-float`、`glow-float-sm`、`glow-ticker`、`glow-pulse-ring`、`glow-safe-fill`、`glow-splash-text/sub/line/rise` |
| 入場工具類 | `.glow-page`、`.g-fade-up`、`.g-fade-in`、`.g-scale-in`、`.g-slide-l`、`.g-slide-r` |
| 錯開延遲 | `.gd-0` ～ `.gd-6`（每階 80ms）|
| 捲動觸發 | `.g-reveal`、`.g-reveal-l`、`.g-reveal-r` + `.visible` |
| 持續動畫 | `.g-float`（5s）、`.g-float-sm`（3.5s）|
| 跑馬燈 | `.g-ticker-track`（28s loop，hover 暫停）|
| Hover 效果 | `.g-feature-card`、`.g-topic-item`、`.g-footer-link`、`.g-nav-link`、`.navbar-scrolled` |
| Tab 切換 | `.g-tab-content`（`glow-fade-in` 250ms）|
| 全域按鈕 | `button:not(:disabled):active { transform: scale(0.96) }` |

#### `hooks/useReveal.js`（新建）

- `IntersectionObserver` 監聽所有 `.g-reveal*` 元素
- 進入視窗後加上 `.visible`，觸發 CSS transition
- `threshold: 0.1`、`rootMargin: '0px 0px -48px 0px'`

#### 各頁面套用

| 頁面 | 套用效果 |
|---|---|
| `App.js` | `key={pathname}` + `.glow-page`（換頁入場）、匯入 `animations.css` |
| `Hero.js` | `g-fade-in`、`g-fade-up`、`g-scale-in`、`g-float`、`g-float-sm`、`g-ticker-track`、`g-reveal`、`g-feature-card`、`g-topic-item` |
| `Dashboard.js` | `useReveal()`、`g-scale-in`、`g-fade-up`、`g-reveal`、`g-tab-content` |
| `ProductDB.js` | CSS 注入動畫（`chipBounce`、`tagSlideIn`、`sectionDown`）|
| `Login.js` | `g-scale-in`、Splash 動畫（`glow-splash-*`）|
| `Register.js` | `g-scale-in` |
| `Navbar.js` | `g-nav-link`、`navbar-scrolled` |
| `Footer.js` | `g-footer-link` |

---

### 6. Navbar 重新設計（`Navbar.js`）

- 改為白底（`#FFFFFF`）+ 底線（`#E5DDD9`），高度 64px fixed
- 捲動超過 10px 加上陰影（`.navbar-scrolled`）
- 未登入：右側「註冊」文字按鈕 + 「登入」深色填充按鈕
- 已登入：頭像圓圈 + 暱稱 + 箭頭下拉選單（`useRef` 外部點擊關閉）
- 下拉選單：前往個人主頁、帳號設定、登出（移除 emoji icon）

---

### 7. 產品資料庫標籤搜尋動畫化（`ProductDB.js`）

- 新增 `FilterChip` 子元件，選取時加上 `chipBounce` 動畫
- 已選標籤顯示於搜尋列下方（附 × 移除按鈕，`tagSlideIn` 滑入）
- 各品項分類列標題顯示篩選數量 badge
- 切換分類時觸發 `sectionDown` 入場動畫（`key={category}`）
- 搜尋列出現清除按鈕
- 移除所有 emoji（`CAT_EMOJI`、分類 icon）

---

### 8. 登入後主頁設計（`Hero.js`）

`Hero` 元件依 `localStorage` 狀態切換顯示 `LoggedInHome` 或 `LandingPage`。

**`LoggedInHome` 版面**

- 歡迎列（深色背景）：依時段顯示 早安 / 午安 / 晚安，膚質 badge，「+ 發布貼文」按鈕
- 可關閉公告條（`announceDismissed` state）
- Tab 列：最新動態 / 熱門貼文 / 追蹤中（`key={tab}` 觸發 `g-tab-content`）
- `PostCard` 元件：頭像、作者 / 系級 / 時間、標籤、Cormorant 標題、截錄、♡ 數 / 留言數 / 分享、熱門 badge
- Sidebar：
  - 近期活動（`EventCard`，緊急活動有紅色強調）
  - 熱門標籤 cloud
  - 快速入口（成分資料庫、個人主頁）
- 活動 Pop（右下角 fixed）：1800ms 後以 `g-scale-in` 浮現，可關閉

**Mock 資料**

- `MOCK_POSTS`：4 篇（油性肌、敏感肌、成分討論、混合性肌）
- `MOCK_EVENTS`：2 筆（保養品交換會、免費膚質分析）
- `MOCK_TAGS`：9 個熱門標籤

---

## 待辦 / 下一步

- [ ] 後端 OTP 寄送 / 驗證 API（由其他同學負責）
- [ ] 後端 API 串接（ProductDB 產品列表、論壇貼文）
- [ ] Dashboard 個人資料編輯功能
- [ ] 社群討論頁面實作
- [ ] Q&A 頁面實作
- [ ] RWD 響應式設計（Mobile 版）

---

*GLŌW Dev Log · Tsai*
