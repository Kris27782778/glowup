# 工作日誌 2026-04-07

**專案：** GLŌW 輔大美妝交流平台  
**負責人：** Tsai  
**日期：** 2026 年 4 月 7 日（一）

---

## 今日完成項目

### 1. 專案整理
- 發現並刪除重複的 `glowup/glowup/` 子目錄（內容與上層完全相同，僅缺 `.env`）
- 確認專案使用 Supabase 雲端資料庫（Project ID：`toqaalnbidqpuwpfuwzr`）

---

### 2. 登入頁面（`Login.js`）— 全新設計

- 改為獨立全螢幕頁面，不顯示 Navbar / Footer
- **左側 55%**：品牌主視覺區（深色底）
  - GLŌW 大字（Cormorant 96px）+ 玫瑰陶土分隔線
  - 義大利斜體副標「了解你擦在臉上的一切」
  - 三大平台特色條列
  - CLARITY IS BEAUTY 底部標語
- **右側 45%**：登入表單
  - 學號 + 密碼輸入，Enter 鍵觸發登入
  - 錯誤提示、載入狀態
  - 連結至註冊頁、回首頁

---

### 3. 註冊頁面（`Register.js`）— 3 步驟流程

**Step 1 基本資料**
- 暱稱
- 系級：4 層串聯選單（學院 → 科系 → 部別 → 年級）
  - 資料來源：`fju_departments.md`（輔仁大學全校科系）
  - 年級依部別自動對應（博士班 → 博一～博四以上 等）
  - 選完後即時預覽「資訊管理學系 · 日間部 · 大三」格式
- Email：分割輸入，`@cloud.fju.edu.tw` 固定顯示，學號自動沿用 @ 前方

**Step 2 帳號設定**
- 學號自動沿用 Email @ 前方輸入，不需二次輸入
- 密碼 + 確認密碼

**Step 3 膚質測驗（`SkinQuiz.js`）**
- 5 題問卷，可複選，依規格 `skin_quiz_spec.md` 實作
- 加權計分，敏感肌優先，混合肌拆分偏乾/偏油
- 每題可略過，亦可跳過整個測驗（後續可在個人頁重測）
- 結果頁：膚質名稱、子類型、描述、特徵標籤

---

### 4. 客製化元件

| 元件 | 說明 |
|---|---|
| `components/Select.js` | 完全客製化下拉選單，符合設計風格 |
| `components/SkinQuiz.js` | 膚質測驗完整元件（含計分與結果頁）|
| `data/departments.js` | 輔仁大學全校科系資料與輔助函式 |

---

### 5. 全站 UI 升級（依 `ui_design_spec.md`）

| 檔案 | 改動 |
|---|---|
| `index.css` | 載入 Cormorant Garamond + DM Sans 字型、全域 reset、自訂 scrollbar |
| `Navbar.js` | fixed 64px→48px 捲動縮小、毛玻璃效果、居中導覽、active 底線、頭像 dropdown |
| `Hero.js` | 全新首頁（Hero 卡片 + 跑馬燈 + 功能區 + 熱門話題 + CTA Banner）|
| `Footer.js` | 深色多欄 Footer，含品牌連結與版權 |
| `Dashboard.js` | 全新個人資料介面（見下方）|
| `ProductDB.js` | 深色搜尋 Hero、sticky 側欄篩選、pill chip 選項 |
| `App.css` | 清除 CRA 預設樣式 |
| `App.js` | 登入/註冊頁獨立，不渲染 Navbar / Footer |

---

### 6. 個人資料介面（`Dashboard.js`）— 全新設計

**版面：左欄 280px + 右欄 flex**

- 深色封面 Hero（240px，含裝飾圓環）
- 96px 頭像，白色邊框浮出封面（修正 z-index 壓層問題）
- 暱稱（Cormorant 28px）+ 膚質 badge
- 系級、Email 資訊列
- 膚質卡（有設定：名稱與保養建議；未設定：開始測驗按鈕）
- 數據卡（貼文 | 追蹤者 | 追蹤中）
- 編輯個人資料 + 登出按鈕
- 右欄 4 個 Tab（帖子 / 問答 / 收藏 / 成分筆記），各有 Empty State

---

### 7. 瀏覽器標籤
- 標題更新為 `GLŌW 輔大美妝論壇`
- Favicon：SVG inline，深色圓角底 + 玫瑰陶土色 `G`

---

### 8. 註冊流程 UX 改善（`Register.js`）

**Step 1 帳號預覽**
- 預覽框標籤從「登入帳號」改為「學號」
- 只顯示 `@` 前方的字串，不再顯示完整 email

**Step 2 密碼強度進度條**
- 密碼輸入時即時顯示強度（5 格分段進度條）
- 計分維度：長度 ≥6、長度 ≥10、有大寫、有數字、有特殊符號
- 等級：非常弱 / 弱 / 普通 / 強 / 非常強，搭配紅→橘→黃→綠色系

**註冊成功確認畫面**
- 原本成功後直接跳轉 `/login`，改為先顯示確認畫面
- 呈現：綠色打勾圓圈、「註冊成功！」標題、帳號資訊卡（暱稱 / 學號 / Email / 系級 / 膚質）
- 使用者確認後手動點「前往登入」

---

### 9. Navbar 重新設計（`Navbar.js`）

**整體風格**
- 從粉紅色系（`#f9f4f4` / `#f4b8b8`）全面換為品牌設計語言
- 背景：白色 `#FFFFFF`，底線 `#E5DDD9`，高度維持 64px fixed
- 字型、色彩與 Login / Dashboard 保持一致

**未登入狀態**
- GLŌW Logo（Cormorant）+ 分隔線 + 導覽連結
- 右側：「註冊」文字按鈕 + 「登入」深色填充按鈕

**已登入狀態**
- 右上角：頭像圓圈（暱稱首字）+ 名字 + 箭頭展開
- 下拉選單：使用者頭像 / 暱稱 / Email + 個人主頁、帳號設定、登出
- 點選外部自動關閉（`mousedown` 事件）

---

### 10. 資料庫連線問題排查與修復

- **問題**：`db.toqaalnbidqpuwpfuwzr.supabase.co` DNS 無法解析
- **原因**：Supabase 免費方案閒置 7 天自動暫停，且連線字串 region 有誤
- **修復**：從 Supabase Dashboard 取得正確 Pooler 連線字串
  - 舊：`db.toqaalnbidqpuwpfuwzr.supabase.co:5432`
  - 新：`aws-1-ap-northeast-1.pooler.supabase.com:6543`
  - User 格式：`postgres.toqaalnbidqpuwpfuwzr`
- 連線測試成功，`skin_types` 資料表正常回傳 5 筆資料
- 後端重啟後登入功能恢復正常

---

## 待辦 / 下一步

- [ ] 後端 API 串接（ProductDB 產品列表）
- [ ] Dashboard 個人資料編輯功能
- [ ] 社群討論頁面實作
- [ ] Q&A 頁面實作
- [ ] 膚質測驗結果寫入 DB（目前僅前端）
- [ ] RWD 響應式設計（Mobile 版）

---

*GLŌW Dev Log · Tsai*
