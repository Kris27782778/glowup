# 工作日誌 — 2026-04-21

**專案**：GLŌW 輔大美妝交流平台  
**負責人**：alfie  
**修改檔案**：`frontend/src/Navbar.js`、`frontend/src/QA.js`、`backend/routes/wishlist.js`、`frontend/src/ProductDB.js`

---

## 本日工作摘要

### 1. Navbar 重新設計（`Navbar.js`）

**整體風格統一**：

- 從舊版色系全面改為品牌設計語言（白底 `#FFFFFF`、邊線 `#E5DDD9`）
- 字型、色彩與 Login / Dashboard 保持一致
- 新增捲動偵測：滾動超過 8px 時套用 `navbar-scrolled` class（加深底色）
- 已登入右上角頭像下拉選單加入使用者信箱顯示

---

### 2. 願望清單功能（收藏 / 取消收藏）

**後端（`routes/wishlist.js`）**：
- `GET /api/wishlist/:user_id` — 取得使用者收藏清單
- `POST /api/wishlist` — 新增收藏（`user_id` + `product_id`）
- `DELETE /api/wishlist` — 取消收藏

**前端（`ProductDB.js`）**：
- 登入後產品卡右上角顯示愛心按鈕
- 點擊即時切換收藏狀態（樂觀更新），失敗時 rollback
- 頁面載入時從 API 同步使用者現有收藏清單

---

### 3. AskQuestion 頁面新增（`AskQuestion.js`）

- 獨立發問頁面：標題、詳細描述、標籤選擇
- 提交後回傳新問題資料至 QA 頁面（透過 `state` 傳遞）
- QA 頁面更新：接收新問題並插入列表頂端，無需重整

---

*GLŌW Dev Log · alfie*
