# 工作日誌 — 2026-04-24 ～ 2026-04-28

**專案**：GLŌW 輔大美妝交流平台  
**負責人**：alfie  
**修改檔案**：`frontend/src/Register.js`、`frontend/src/ProductDB.js`

---

## 工作摘要

### 1. 產品資料庫搜尋框修復（Apr 24）

- 修復搜尋框入力後畫面不更新的問題
- `Register.js` 統一改用 `API_BASE`（原本混用 hardcode `localhost:5001`）

---

### 2. API 路徑調整與 Revert（Apr 28）

**問題**：前一版 commit 誤將 API 路徑改為 `localhost`，並移除 `search` 參數，導致搜尋功能失效。

**處理**：
- 執行 `git revert`，恢復正確的 `API_BASE` 寫法與 `search` 查詢參數

---

*GLŌW Dev Log · alfie*
