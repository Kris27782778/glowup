# 工作日誌 — 2026-04-22 ～ 2026-04-23

**專案**：GLŌW 輔大美妝交流平台  
**負責人**：alfie + 協作  
**修改檔案**：`backend/routes/questions.js`、`backend/routes/answers.js`、`backend/routes/auth.js`、`backend/index.js`、`frontend/src/QA.js`、`frontend/src/AskQuestion.js`、`frontend/src/Register.js`、`frontend/src/Dashboard.js`、`frontend/src/Hero.js`、`frontend/src/Admin.js`、`frontend/src/config.js`

> 此兩日為本週最高強度開發日，多人協作並多次 merge conflict 解決。

---

## 工作摘要

### 1. 問答系統完整後端串接（Apr 22）

**功能實作**：
- 前端 QA 頁面改為從 `GET /api/questions` 拉取真實資料
- 使用者可發問、刪除自己的問題
- 回覆功能：`POST /api/questions/:id/answers`
- 後端 join users 資料表，回傳用戶暱稱與系級資訊

---

### 2. 匿名發問系統（Apr 23）

**後端**：
- `is_anonymous` 欄位：匿名問題隱藏 user_id，但保留用以內部識別（顏色固定）
- 新增 `include_anonymous` 查詢參數，控制 API 是否回傳匿名問題

**前端**：
- 發問頁新增匿名切換開關（含遮罩圖示與提示文字）
- 頭像顏色依 user_id hash 固定，匿名者顯示「？」
- 自己發的匿名問題儲存至 `localStorage`，在列表中可識別並刪除
- 問題列表顯示用戶暱稱、系級；匿名問題顯示「匿名用戶」

---

### 3. 電子郵件驗證 OTP（Apr 23）

- 後端新增 OTP 生成與寄送邏輯（`nodemailer` + Gmail SMTP）
- 寄至 `@cloud.fju.edu.tw` 信箱
- 驗證碼 6 碼數字，有效期限設定

---

### 4. 註冊流程改善（Apr 23）

- 新增「姓名」欄位（顯示用，不影響登入帳號）
- 自動登入：完成膚質測驗後直接登入，不需跳轉登入頁
- 膚質測驗紀錄管理：避免重複寫入

---

### 5. 管理後台（`Admin.js`）— 初版（Apr 23）

- 需管理員 KEY 驗證進入（`ADMIN_KEY` 環境變數）
- 三大模組：
  - **用戶管理**：搜尋、封鎖 / 解鎖用戶
  - **產品管理**：查詢、啟用 / 停用產品
  - **問題管理**：查詢、刪除問題
- 前端分頁（API 後端分頁待後續實作）

---

### 6. 主頁輪播 Banner（`Hero.js`）（Apr 23）

- 新增 4 張輪播幻燈片（活動資訊、功能介紹、精選文章、產品資料庫）
- 自動輪播（5 秒）、暫停 hover、點指示器、上下頁箭頭
- 淡入淡出 + translateX 過場動畫

---

### 7. API 環境設定集中化（Apr 23）

- 新增 `frontend/src/config.js`：`API_BASE = process.env.REACT_APP_API_URL || 'http://localhost:5001'`
- 全站所有 fetch 改用 `API_BASE`，統一管理開發 / 生產環境切換
- 新增 `frontend/vercel.json`：設定 `outputDirectory: "build"`

---

### 8. 儀表板收藏功能（Apr 23）

- Dashboard 收藏 Tab 串接真實 API
- 支援在 Dashboard 直接取消收藏，即時更新畫面

---

## 解決的問題

- 多次 merge conflict（`QA.js`、`AskQuestion.js`、`Register.js`）手動解決
- API 路徑統一（各頁面原本混用 hardcode `localhost`）

---

*GLŌW Dev Log · alfie*
